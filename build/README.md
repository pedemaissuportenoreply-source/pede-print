# build resources (electron-builder)

Drop the app icon here as `icon.ico` (256×256 multi-size ICO).
electron-builder auto-detects `build/icon.ico` — no config change needed.
Until then, the default Electron icon is used and the installer still builds.

Optional: `installerIcon.ico`, `uninstallerIcon.ico` for custom NSIS icons.
